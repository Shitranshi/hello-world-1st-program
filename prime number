#include<stdio.h>
int main()
{
	int n;
	scanf("%d",&n);
	primenum(n,n/2);
	
} void primenum(int x,int y)
{
	if(y<3)
{
		printf("prime number");
	return;}
	if(x%y==0)
	{
		printf("not a parime number");
		return;
	}
	primenum(x,--y);
}
